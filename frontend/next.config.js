/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Future configurations can be added here.
  // For example, if using `src` directory but pages is not automatically picked up,
  // you might need to specify:
  // pageExtensions: ['tsx', 'ts', 'jsx', 'js'], // default
  // By Next.js convention, if `src/pages` exists, it's used automatically.
};

module.exports = nextConfig;
